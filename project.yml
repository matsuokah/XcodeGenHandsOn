# プロジェクト構成・設定
name: Sample
configs:
  Debug: debug
  Release: release
options:
  groupSortPosition: top
attributes:
  ORGANIZATIONNAME: matsuokah
fileGroups:
    - Sample
    - SampleTests

# ターゲット
targets:
  Sample:
    type: application
    platform: iOS
    sources:
      - path: Sample
    configFiles:
      Debug: Sample/Debug.xcconfig
      Release: Sample/Release.xcconfig
  SampleTests:
    type: bundle.unit-test
    platform: iOS
    dependencies:
      - target: Sample
    settings:
      TEST_HOST: $(BUILT_PRODUCTS_DIR)/Sample.app/Sample
      INFOPLIST_FILE: SampleTests/Info.plist
    sources:
      - SampleTests

# スキーム
schemes:
  Sample:
    build:
      targets:
        Sample: all
    run:
      config: Debug
    test:
      config: Debug
      gatherCoverageData: true
      targets:
          - SampleTests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
